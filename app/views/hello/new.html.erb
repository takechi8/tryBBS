<div id="main">
<%= form_for(@hello) do |f| %>
   <% if @hello.errors.any? %>
     <div id="error_explanation">
       <h2><%= pluralize(@hello.errors.count, "error") %> prohibited this book from being saved:</h2>
 
       <ul>
       <% @hello.errors.full_messages.each do |message| %>
         <li><%= message %></li>
       <% end %>
       </ul>
     </div>
   <% end %>
		<div>
			<%= f.label :name%>
			<%= f.text_field :name%>
		</div>
		<div class="actions">
			<%= f.submit%>
		</div>
	<% end%>
</div>

